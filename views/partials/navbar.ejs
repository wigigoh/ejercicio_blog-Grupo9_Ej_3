<nav class="navbar bg-dark fixed-top">
  <div class="container px-5">
    <a class="navbar-brand mb-0 h1 text-white" href="/">Hack the blog</a>
    <div class="d-flex align-items-center">
      <% if (locals.user) { %>
      <!-- <a class="text-white btn m-1" href="/"
        ><%=// user.authorFirstname %> <%= //user.authorLastname %></a
      > -->

      <div id="delUser" class="dropdown-center me-2">
        <p
          class="dropdown-toggle text-white"
          type="button"
          data-bs-toggle="dropdown"
          aria-expanded="false"
        >
          <%= user.authorFirstname %> <%= user.authorLastname %>
        </p>
        <ul class="dropdown-menu bg-danger">
          <li>
            <form action="/usuarios/?_method=DELETE" method="post">
              <button class="dropdown-item bg-danger text-white">Eliminar Cuenta</button>
            </form>
          </li>
        </ul>
      </div>

      <% if (locals.user.role.code < 400) { %> <% if ( locals.user.role.code < 200) { %>
      <a class="btn btn-secondary m-1" href="/panel-usuarios">Panel de usuarios</a>

      <% } %>
      <a class="btn btn-secondary m-1" href="/admin">Panel de articulos</a>

      <% } %>

      <a class="btn btn-outline-danger m-1" href="/logOut">LogOut</a>

      <% } else { %>
      <a class="btn btn-outline-primary m-1" href="/usuarios/registro">Sign Up</a>
      <a class="btn btn-success m-1" href="/login">Login</a>
      <% } %>
    </div>
  </div>
</nav>
